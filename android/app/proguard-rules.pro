# Faz 7 (review §8 "R8") fills this in with real keep rules once there's enough surface
# (Hilt/Compose/kotlinx.serialization/supabase-kt/RevenueCat) to know what actually breaks
# under minification. Empty for now — release build type has isMinifyEnabled=true so this
# file must exist, but no rules are needed yet at skeleton stage.
