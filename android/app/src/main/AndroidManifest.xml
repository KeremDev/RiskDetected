<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- required=false: System Photo Picker (master §15.3) is a valid capture path without a
         camera, so this must stay installable on camera-less devices (e.g. Chrome OS). -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".RiskDetectedApplication"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.RiskDetected"
        android:supportsRtl="true">

        <!--
          Ç1 (review doc): no certificate pinning in v1 — Network Security Config +
          HTTPS-only + system CA store is the accepted v1 baseline. android:allowBackup is
          false so no auto-backup can exfiltrate the encrypted session store (master §25.4).
        -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.RiskDetected">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Generated-report files (currently only the generate-excel-report .xlsx download,
             feature:reports's HistoryViewModel) are written to cache and handed to a viewer/
             share target via a content:// Uri — file:// Uris are blocked across app boundaries
             on modern Android (FileUriExposedException), so a FileProvider is required. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- FCM push registration — see RdFirebaseMessagingService's doc comment for what's
             actually wired vs. still Faz 7 scope (backend FCM sending). -->
        <service
            android:name=".push.RdFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>
</manifest>
